---
description: Launch Rebalance, select a portfolio, or operate it through the shared skill
---
Use the project's skills/rebalance/SKILL.md for Rebalance.

Requested arguments: $ARGUMENTS

A bare /rebalance is handled by the native Rebalance plugin before this message reaches the model. Open its returned linked selector and report readiness briefly in your own voice, with any real startup blocker. Previously running portfolios restore independently of the chat selection; explicitly stopped ones stay stopped. Do not repeat launch/start/recovery. If no native result is present, the shared skill can open the selector through setup-only; do not claim background restoration or substitute a model-driven full launch. Nonempty arguments request only their named operation. Wallet setup never authorizes trading.
