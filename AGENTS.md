# Project instructions

This is an ETHOnline 2026 **Start from Scratch** project. Read `PLAN.md`, `docs/HACKATHON.md` and `docs/AI_USAGE.md` before implementation. The latest MVP removes spending limits and generic infrastructure while retaining Ledger/Privy features for their prize demos.

- Preserve meaningful hackathon history and commit material prompts, specs, plans, AI disclosures and dependency provenance. Do not backdate or squash away the work.
- The owner uses `main` directly. Follow that preference and the session's commit/push authorization.
- Portfolio selector, Back navigation and New wallet signer-choice clicks are explicitly allowed. They select this conversation or run deterministic local wallet onboarding. New raw-key accounts on macOS share one Keychain-held BIP-39 seed using consecutive browser-wallet address indexes; only public references/reservations are stored in the project. Preserve any existing legacy wallet identities. Never use a file/env fallback for a Keychain wallet. See docs/MACOS_KEYCHAIN.md. The original standalone wallet was retired after the documented test incident. Privy must use agents.privy.io and its official Agent Sandbox flow exclusively: no developer dashboard, app secret or alternate app API integration. Ledger verifies an indexed address on the existing device seed. Navigation and wallet setup clicks do not arm trading. Explicit user Start/Stop clicks on the chart now control that displayed portfolio deterministically; see `docs/prompts/050-portfolio-controls.md`. The top public wallet address opens that displayed wallet on the Robinhood mainnet explorer. See `docs/WALLET_SETUP.md`. Keep the companion in a persistent host Browser pane; see `docs/COMPANION_VIEW.md`. Allocation changes go through the agent; the companion additionally permits explicit Start/Stop, Ledger Retry for a suspended request, public explorer navigation and copying a strategy-only share code. Share codes contain targets, drift trigger and cycle interval, never wallet addresses, holdings, signers, RPCs or secrets; import previews through the agent before an explicitly requested apply. The portfolio display is the PR #20 ring monitor: outer actual holdings, thin inner targets with matching colors, ticker/actual/target labels, and current execution/drift status in the center. Its center shows status; a compact Settings disclosure holds the drift trigger, cycle interval and optional per-wallet rebalance fee target. Stock segments and labels are native Google-search links: hover/focus emphasizes actual, target and name together with outward motion, disabled under reduced-motion preferences. Ring gaps have parallel edges. Gas prices appear only when blocking on the fee target. ETH remains outside allocation slices and weights. Read [fee estimation](docs/FEE_TARGET.md): fresh execution-specific Coinbase ETH/USD plus RPC gas and deterministic remaining-leg estimates gate signing; unavailable or excessive fees wait locally, without chat alerts. Existing display caches never feed signing. This is an approximate prospective fee check, not actual-spend accounting, a fee guarantee or a generic budget engine. Gas abstraction was removed under [prompt 068](docs/prompts/068-remove-unused-paymaster.md); native ETH remains required. Paymaster setup/execution documents are archival. Do not interpret old gasPayment or pending transport fields as native transactions or remove those records automatically.
- Bare native skill entry opens the linked portfolio selector and restores only saved running preferences, independent of this chat's wallet attachment. Explicitly stopped, never-started and unknown portfolios remain stopped. Actual Start records enabled intent; explicit Stop records disabled intent and keeps precedence over in-flight restoration. Process exit retains intent for a later user invocation. Adopt legacy intent only from a live owned runner with matching public wallet/chain and no Stop, never cached armed state alone. App-entry snapshots freeze eligible wallets plus preference and Stop generations; old single-wallet/selection-only hook receipts keep their original meaning. Card selection immediately connects the chat without another invocation and remains non-trading. Use a brief natural ready/pick response; actual startup or view failures remain visible. Missing hook output is not proof of trust state. App-entry `restorationResults` is not wallet inventory. Open a ready linked browser view before replying and retry only read-only view preparation for a proven local-access denial; see [prompt 086](docs/prompts/086-deterministic-selector-presentation.md). See [prompt 085](docs/prompts/085-restore-running-portfolios.md). Explicit per-wallet Start/launch and `launch --all` retain their requested scope, with physical confirmation for every Ledger transaction. Setup-only never restores inactive runners. The agent must not claim that preparation fulfilled a blocked restoration or route financial activation through another agent/schedule.
- App-entry restoration is deterministic in `src/app-launch.ts`; per-wallet startup is in `src/launch.ts`. A user-reviewed Codex prompt hook handles bare `$rebalance`, typed or submitted as the exact Markdown reference to this repository's canonical `skills/rebalance/SKILL.md`, including the exact observed browser-context framing around that complete request. Browser metadata never supplies launch authority. Armed raw-key recovery is automatic after a 30-second stale-send grace; the separate exact `$rebalance recover` command remains explicit intervention. Read `docs/RECOVERY.md`. If the hook already returned a result, the agent must not repeat launch/start/recovery. Scoped prompts and unrelated links do not launch. Use `launch --setup-only` for permitted preparation. Preserve invocation deduplication, newer stop requests and unknown-start reporting. Claude uses a separate exact bare `/rebalance` `UserPromptExpansion` handler with native `prompt_id`, never a model Skill call or duplicate submit hook. Read `docs/LAUNCH.md`; never trust or trigger the funded hook on the user's behalf, change hook trust/approval settings, or claim desktop hook dispatch from fixture tests.
- OpenCode uses the checked-in command/plugin, verified against 1.18.30. Only a native bare `/rebalance` in a verified root Build session dispatches through `scripts/rebalance-opencode-hook.mjs`; native command provenance and the resolved message ID are required. Run the shared launcher in Node, not OpenCode's embedded Bun runtime. The plugin supplies `opencode:<sessionID>` to shell commands and connects public views and actionable events to that session. Preserve paused preferences, wallet scope, immutable hook routes and uncertain-send barriers. Never substitute the Codex notification CLI for OpenCode's native `notifications pause/resume/status` arguments. Loading a skill, a notification, a subagent or Plan mode is not launch authority. See [OpenCode integration](docs/OPENCODE.md). No global harness trust/permission changes, model-choice override, MCP relay or periodic model task is required.
- Use one existing agent conversation and the project Rebalance skill. The app runs an explicit deterministic graph. Claude MCP events and `/rc`, or a file-driven Codex notification worker using native `codex queue --thread UUID` and Remote, sit outside trading. Target only this verified existing conversation. Native cross-process queueing reads metadata and shared queue storage without loading/resuming its target or taking its writer; do not add a custom relay or direct resume. Codex has its own ten-second revision check; do not claim the entire host path is event-only. Preserve paused notification preferences. Native notification delivery into this conversation was verified on September 6; the five-minute current-chat heartbeat was then deleted. Do not recreate a periodic model check. No signing/permission relay is permitted. Retain undelivered completion/recovery/Ledger/runtime-attention events, report meaningful new events only and do not claim phone delivery from a transport write or scheduled check.
- The current demo assets are USDG, AAPL, NVDA, MSFT and AMD on Robinhood. Configure USDG plus four stocks from the verified manifest; previous TSLA/AMZN/RUN/MRNA configurations remain supported. Native ETH is gas-only; WETH is excluded. Read `docs/RWA_CHECK.md` for canonical routes, DEX-unit valuation, multiplier/pause semantics and issuer limitations. Use user-provided weights or an explicit delegation to choose demo weights. Read `docs/DEMO_PORTFOLIO.md` for the current selection; do not represent the theme as an audited ethical or anti-monopoly screen.
- Raw-key and Privy profiles execute automatically with no per-trade human input or LLM calls. Ledger Start/bare launch enables direct device-confirmed rebalancing: it tracks drift from its saved public address while disconnected, then prepares approvals/swaps and prompts the connected device directly when conditions permit. No compulsory per-rebalance agent request is needed; every transaction requires physical confirmation. The backend creates and consumes one bounded durable request tied to one wallet/config/runner/cycle. Restart never replays consumed work. Rejection, timeout or execution failure durably suspends repeat prompts until an observed disconnection/reconnection, an explicit chart Retry click, or `ledger rebalance --request-id <UUID>` on the running portfolio, with fresh preparation and all receipt/cadence checks. Chart Retry is tied to the displayed, attached wallet and exact finished request; it needs a running portfolio and connected device, preserves pending transactions, and never starts a runner or changes blind-signing refusal. Stop/configuration changes invalidate unbroadcast work. Selection, setup and notification-only turns never initiate trading. `ledger status` reads the public request; ordinary `status` includes prompt suspension. Ledger cancellation signing is disabled; unresolved hashes remain receipt barriers and confirmed reverts need explicit existing receipt recovery. Read `docs/LEDGER_EXECUTION.md`. Hardware onboarding, the September 12 device-confirmed 1.1875 USDG approval and a later complete sequential Ledger rebalance are verified. The final swap was mined at 2026-09-12T23:25:06Z in block 61493058; the app recorded completion at 23:25:08Z and fresh holdings were within the drift threshold. See [canonical Ledger evidence](docs/LEDGER_EXECUTION.md). Preserve the earlier unsupported blind-signing attempt as dated history, not the current outcome. Clear Signing, explicit rejection behavior and live execution of the new multicall batches remain unverified; Key Ring remains separate.
- Production rebalance preparation combines sales and purchases in one atomic multicall under [prompt 083](docs/prompts/083-atomic-sales-and-purchases.md). Quote and final construction independently read actual holdings at one fresh block; sales precede purchases, each stock/pool is used once, and purchase inputs are bounded by held USDG plus encoded sale minimum outputs less the conservatively rounded cash reserve. Outputs go directly to the wallet. Never treat projected balances as observed holdings or use optimistic proceeds as spending authority. Exact aggregate approvals remain separate per deficient input token. Context-free explicit phase APIs remain for compatibility. Preserve the prompt082 residual rule, pending receipt barriers, successful-swap cadence, per-leg minima/common expiry, Stop/settings checks and Ledger physical confirmation/fallback behavior. A mixed portfolio with sufficient allowances can use one swap transaction; do not promise one total transaction regardless of allowances or market changes. Actual gas and displayed trade counts must cover the full prepared plan. Live combined execution remains separate from the verified sequential Ledger result. See [batching](docs/BATCHED_REBALANCES.md).
- Cycles with a successful swap retain one hour between starts, with a fixed ten-minute active window for sequential legs and a five-percentage-point drift trigger. New cycles without a successful swap may retry after their original ten-minute window; legacy records remain conservative. Persist successful-swap evidence before clearing pending through every receipt path. Persist cycle timing across restarts and target edits; reconcile pending receipts before any interval gate. Polling and notification checks never bypass this cadence. Do not represent it as a spending cap or one-transaction-per-hour limit.
- Use event-driven serial scheduling: sequencer activity hints, local control-file events and exact deadlines, with bounded receipt/control watchdogs and RPC-error backoff. Reuse verified discovery with bounded expiry; keep quotes, balances and corporate-action reads fresh. The chart receives local SSE status updates with polling only as fallback. Read `docs/EXECUTION_TIMING.md`; event hints never establish receipt/finality or bypass cadence.
- Keep the shared app small: one TypeScript project, one active profile/chain, ordinary local config, a basic CLI and a pending-transaction record. No spending caps, budget accounting, generic policy/permission engine or bespoke authentication framework.
- Retain Ledger Key Ring with a focused credential-broker demo and Privy-native scoped authorization where useful for prizes. Use service/contract/method restrictions rather than monetary caps. Verify actual allowed/denied behavior and isolation claims; do not generalize these integrations into a security platform.
- No session keys, delegation modules or session feasibility work. Prioritize current Ledger Agent Stack compatibility and direct signing. Refresh balances/quotes on reconnect, reconcile earlier sends, distinguish hardware detection from app/account readiness and avoid repeated prompts. Retain notifications while the agent is unavailable.
- Retain correct swap mechanics: integer amounts, valid target weights, correct network/assets/recipient, usable prices/routes, normal slippage/expiry, sufficient balance/gas and receipt tracking before another trade. Do not silently switch signers/accounts or duplicate unresolved transactions. Automatic recovery uses the existing runner lock, one same-nonce cancellation and both saved hashes; no self-stop/restart or LLM. Confirmed cancellation/revert may continue the current active window; it does not impose an hourly wait on a new cycle without a successful swap. Unknown cancellation outcomes remain receipt-only.
- Cloud LLM assistance and Privy's TEE are accepted. Keep secrets outside chat, source control and logs using local/provider setup or the focused Ring integration.
- Maximize applicable Ledger Agent Stack reuse and pin adopted versions/licenses. Read `docs/LEDGER_AGENT_STACK.md`; shared EVM config includes Robinhood, and the CLI quote guard is not a blanket chain restriction.
- Robinhood mainnet 4663 is selected. The user accepted its measured fees in `docs/prompts/017-mainnet-cadence-codex.md`, canceling the conditional testnet migration; preserve `docs/FEE_CHECK.md` as historical evidence. A user-issued native hook armed the funded raw-key runner on September 5 and its Apple swap receipt was verified; later user-issued recovery/relaunch resolved the pending barrier, and the app recorded full five-asset completion at 2026-09-05T23:33:58.399Z. Public RPC verified its final swap receipt at block 55516741; refreshed holdings were within the drift threshold. The single skill launch request includes arming, but the current assistant cannot activate real-money trading. Read `docs/NETWORK.md`, verify Robinhood assets/routes and preserve truthful stock/liquidity/verification claims. If a route is unavailable, try another live pair on Robinhood or report the gap. No alternative chain or mock-stock/test-pool demo fallback; local unit/fork tests remain development checks.
- Portfolio settings/targets/policies are editable while running; use [live settings](docs/LIVE_SETTINGS.md), preserving pending receipts and recorded cycle timing. Code upgrades still require loading new process code.
- Run tests through `npm test` (the isolated launcher), or provide explicit temporary ROOT/DATA directories before Node starts. Never run a fixture with application data paths; see [the test-isolation incident](docs/TEST_ISOLATION_INCIDENT.md).
- Keep application storage/calculation/chart local, bundle UI assets and avoid application telemetry. Disclose external RPC, Ledger or Privy dependencies accurately.
- Test the core arithmetic, automatic/device-confirmed execution and basic recovery. Record actual results; no invented hardware, network or sponsor evidence.

User instructions supersede project preferences. Published competition requirements still apply; distinguish them from optional product scope.
