# Claude Code project context

Read [AGENTS.md](AGENTS.md), [PLAN.md](PLAN.md) and [docs/HACKATHON.md](docs/HACKATHON.md).

A user-issued bare `/rebalance` requests setup and arming together through the skill in this conversation, reusing wallet/configuration/chart and previously requested notifications. No separate arming message is required. Specific setup-only/status/event/stop requests do only that operation. Respect the executing agent's financial-action restrictions and report an incomplete, unarmed launch when they prevent starting an inactive runner; reuse an already active runner and report its actual state. Do not route around restrictions or call setup alone a completed launch when arming remains blocked.

Use `npm run cli -- launch` for the deterministic startup sequence where execution is permitted, or `launch --setup-only` for preparation. Do not reconstruct startup decisions in prose or repeat a launch already handled by a hook. The project prompt hook targets Codex's bare `$rebalance`, typed or submitted as the exact canonical project-skill reference from its picker, including the exact observed browser-context framing around the complete request. Browser metadata never supplies launch authority; no Claude prompt hook is installed. Preserve newer stop requests, pending/cycle records and truthful unknown-start reporting. See [launch behavior](docs/LAUNCH.md). Codex also recognizes the separate exact `$rebalance recover` command for explicit cancellation; `recover` alone is a read-only CLI assessment. Read [recovery behavior](docs/RECOVERY.md), preserve both identities and never repeat a handled recovery or trigger the funded hook on the user’s behalf.

Build the smallest working rebalancer: one TypeScript app, local config, deterministic loop, three signer implementations and a view-only local chart. Raw-key/Privy swaps run automatically without human or LLM input. Ledger tracks drift while disconnected, refreshes on connection and prompts through the agent to rebalance with physical confirmation. Connection alone never authorizes signing. All app commands go through the agent.

Default cycle starts are one hour apart, with a fixed ten-minute active window for sequential approval/swap legs and a five-percentage-point drift trigger. Pending receipts reconcile first. Cycle timing survives restarts and target edits; the 30-second observation poll does not authorize a new cycle every poll.

No session keys, delegation modules, spending caps, budget counters, generic policy engine or custom custody vault. Keep Ledger Key Ring/credential-broker and Privy-native scoped authorization features for their prize demos, with focused allowed/denied-operation evidence and no monetary limits. Maximize current Ledger Agent Stack compatibility through direct signing. Keep basic correct swap arithmetic, slippage, balance/gas and pending-transaction handling. Keep secrets out of chat/Git/logs.

Reuse applicable Ledger Agent Stack components; defer hardware testing/enrollment until arrival. Robinhood mainnet (4663) remains selected. The [fee decision](docs/prompts/017-mainnet-cadence-codex.md) accepts the measured fees and cancels the conditional testnet migration; preserve [the fee research](docs/FEE_CHECK.md). The user-started raw-key runner completed an Apple swap before the next transaction became unresolved; the full rebalance is incomplete. The [launch clarification](docs/prompts/019-single-skill-arming.md) includes arming in the skill request and distinguishes the current assistant's execution restriction from product scope. No alternative chain or undisclosed mock-stock substitution; keep local correctness tests. Use Robinhood's own token/price semantics. Cloud LLM input and Privy's TEE are accepted. Preserve hackathon history and [AI/provenance records](docs/AI_USAGE.md).

Operational entrypoint: use the project `rebalance` skill in this same conversation. The current demo assets are USDG, AAPL, NVDA, MSFT and AMD (see `docs/DEMO_PORTFOLIO.md`); configure USDG and four known stocks from the manifest; native ETH is gas-only. Preserve saved user-selected weights unless instructed otherwise. `npm run cli -- status`, `graph` and `events` return public local state. The optional `rebalance-events` MCP channel only reports retained notifications and acknowledges session handling. `/rc` phone use needs the user's Claude setup. Codex uses native Remote for its existing conversation and a native five-minute current-chat heartbeat that only checks retained events; no custom app-server bridge is required. Trading is independent of both notification paths. No live mobile delivery or Ledger/Privy execution is claimed. See `docs/NOTIFICATIONS.md`.
