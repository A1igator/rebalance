# Funding clarification and fee-triggered testnet choice — 2026-09-04

Exact material human instruction:

> double check robinhood chain fees are near 1 cent otherwise let's switch to testnet before full demo since chain seems congested

The user initially said “did 1” in response to funding instructions, then clarified:

> oh wait sorry I didn't send just bridged in my own wallet. will send in a second

The subsequent message was:

> sent now

The latest instruction supersedes the earlier prohibition on testnet when its fee condition is met. Robinhood remains the only chain family. It does not establish that the existing mainnet stock contracts or pools exist on testnet, or select an undisclosed mock-stock fallback.

Codex used the project Rebalance skill for public status and a read-only observation/plan/quote. It confirmed mainnet funding and an unarmed runner. Public fee research used mainnet gas estimation, recent canonical-router receipts and an ETH/USD spot reference. Wallet-specific funding details are omitted from committed evidence; no private key was inspected or transaction submitted.

Material delegated research, summarized: verify official Robinhood testnet identity, canonical test USDG, the exact four selected stock tokens and usable Uniswap routes. Check published sources and bounded public RPC observations without signing, changing app configuration or treating absent registry entries as proof of nonexistence. A follow-up checked independently published factory/quoter/router addresses, onchain identities, stock metadata and the faucet. The research agent retained public observations; root summarized bytecode responses with hashes for the repository.

A separate reviewer checked active network documents for stale mainnet-only prescriptions and claims that would incorrectly imply working testnet stock support. Root updates the plan, network report, contributor instructions, README, fee report and AI disclosure. Application code, allocation, signing mode and runtime chain remain unchanged.

The agent asked the user to choose between clearly labeled demo-issued stock tokens/new USDG pools on Robinhood testnet and the existing issuer-backed mainnet stocks at the measured fees. That choice is pending; neither an accepted mainnet fee exception nor permission to replace the asset provenance is inferred from elapsed time.
