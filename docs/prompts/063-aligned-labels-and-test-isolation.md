# Aligned labels and isolated validation — 2026-09-10

The human reported that labels no longer consistently line up with their ring segments, including USDG appearing beside Apple. Align each label with its corresponding visible actual segment (or target when there are no holdings), retain synchronized emphasis, and avoid overlapping labels. Record relevant geometric regression checks and inspect the actual companion.

## Test incident and containment plan

A new static test import caused configuration paths to initialize before the fixture environment was set. Transaction-test setup wrote public fixture values into the real raw-wallet config/key/pending/last-transaction paths. The real runner had already been paused with explicit user approval. Tests used mocked signing/transport; they did not broadcast live transactions. Test execution was stopped when the isolation failure was recognized. The user reports no backup of the autogenerated standalone key. Do not generate a replacement for that address or claim the original key recovered.

Preserve the original wallet metadata, holdings/status, cycle, recovery and stop records. Quarantine the four fixture-contaminated files without exposing their contents. Restore only public configuration and last-operation evidence that can be established from existing records; retain the original address and leave signing unavailable without its original key. Keep other wallets untouched. Record actual restoration evidence and limitations.

Before further tests, remove import-time configuration dependencies from the shared lock helper, require explicit data directories, assert isolated paths before fixture writes, and add a test bootstrap that chooses temporary ROOT/DATA directories before any app import. Add an application-side check in the test process that rejects non-temporary/default live data paths. Run offline checks with explicit temporary environment and document the incident and exact validation results. No live trading or new wallet creation is part of containment.
