# Make retry follow the required fix

September 12, 2026. Recorded before implementation.

The owner clarified that an icon next to the prerequisite text makes it look as though retry itself resolves the problem. Restore a continuous instruction such as “Open Ethereum on device, then retry”, with only the word “retry” acting as the existing button. Remove the icon. Keep one action, its wallet/request guards, accessible label, tooltip and visible pending feedback. Hide its connector with the action when unavailable; preserve natural sentence wrapping and the portfolio value below. No hardware, portfolio settings or trading behavior changes.

Update the existing isolated UI checks, inspect the real chart without clicking the live action, and commit/push to main under the standing preference. Preserve unrelated stock-link edits. The required Tenjin search returned NETWORK_ERROR.
