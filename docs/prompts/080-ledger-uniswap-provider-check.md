# Check Ledger’s Uniswap provider on Robinhood

September 12, 2026. Recorded before live provider checks.

The owner asked to verify whether the dedicated Ledger Wallet CLI Uniswap flow can support Rebalance’s Robinhood mainnet USDG and stock tokens with readable device signing. Compare the current official CLI/source contract, resolve the real chain-specific token identifiers and run bounded read-only quote/build checks using the documented request shapes. Do not fabricate provider quotes, token identifiers or Clear Signing metadata. Distinguish local command guards, catalog coverage, service availability and actual device display.

Use only public source/token data and credential-free supported requests. No signing, broadcasts, approvals, wallet/configuration changes, runner restart or automatic retry occurs during this service check. If a valid quote/build succeeds, inspect its chain, assets, amount, recipient, deadline/slippage and signing app before deciding the next device-validation step. Keep full private runtime data and view links out of committed evidence. Record actual outcomes, remaining gates and source versions; preserve unrelated stock-link edits. The required Tenjin search returned NETWORK_ERROR.
