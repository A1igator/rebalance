# Standalone Calibur setup before filming

The owner accepted live Calibur preparation, then clarified: “can we just do the authorization now even if no rebalance is due? so for demo everything is one transaction?”

Add an explicit standalone Ledger setup operation that installs the pinned Robinhood Calibur delegation in one self-funded type-4 transaction, with the chain-specific authorization and outer transaction confirmed on the device. The outer call is the canonical root execute overload with an empty call list and revertOnFailure true. It cannot approve tokens, swap, alter allocations, start trading or change cadence. Signing an authorization alone does not install it; verify an onchain receipt and the exact current delegation before claiming activation.

Require a saved Ledger/Calibur selection and an idle portfolio. Hold the run lock; preserve a pre-existing Stop but abort on newer Stop/configuration changes. Simulate the full empty call using an unsigned delegation override, account for authorization gas, enforce the existing configured fee target and native balance, verify nonce, deployment and both signatures, and retain the normal pending-before-send barrier under a distinct calibur-setup kind. Existing pending work is reconcile-only. A successful setup is not a swap or a completed rebalance and never changes the cycle. A rejected device request or uncertain broadcast must not repeat automatically.

Keep an explicit setup CLI separate from ordinary rebalance commands and provide a read-only status/preflight operation. Keep notifications paused. Tests use disposable local state and existing canonical-runtime fixtures, never live wallets. The assistant may stop and prepare the intended portfolio; the user initiates the live onchain setup and physically confirms. Record hardware and receipt evidence only after actual verification.

The owner then requested: “this should be done on start button click with a button status change and on hover explaining what callibur is if a wallet doesn't already have it setup eh”. Integrate the standalone operation into the existing explicitly clicked Start control for a stopped Ledger portfolio. Preserve the wallet-bound request journal and original Stop generation. Opt into Calibur under the idle/config locks, submit setup at most once, and start only after receipt/delegation verification. Display setup progress on the button and explain first-use Calibur in its hover text; already installed wallets start normally. Do not alter active runners or raw-key/Privy starts. Preserve unknown-outcome barriers, notification pause, targets and cadence.
