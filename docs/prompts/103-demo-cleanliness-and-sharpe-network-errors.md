# Demo cleanliness and actionable Sharpe network failures

Date: September 13, 2026.

The owner reported another failed Sharpe request in a referenced app conversation. The command correctly resolved the selected wallet and asked its bundled assumptions question. After the owner confirmed, the public-history fetch failed and returned only a generic history-unavailable response. The owner also clarified that Tenjin must be fully disabled for the demo, including searches rather than publishing alone.

## Scope

Preserve bounded public-history failure causes as fixed structured data: local access denied, network unavailable, timeout, provider HTTP failure or invalid history. DNS failure alone must not be called proof of sandbox denial. Never expose remote response bodies, raw exception messages, credentials or arbitrary URLs. Failed history fetches remain before solving, locking or applying configuration.

Guide the agent to retry a proven prewrite network failure at most once through the host's approved network permission path when appropriate, preserving the returned wallet, preset and preview/application intent. Reuse already-authorized assumptions. Do not repeat unknown or already-applied outcomes; inspect public state first. Provider HTTP/schema problems retain their own cause and must not trigger indiscriminate permission escalation. No automatic permission changes, financial activation, source substitution or unbounded retry is added.

Use Tenjin's supported local uninstall to remove its agent integration, hooks and daemon for the demo, retaining its CLI, wallet, configuration and library. Verify the result. Do not reinstall, search or publish during the demo unless the owner explicitly re-enables it. Existing unrelated application hooks remain intact. Previously injected instructions remain conversation history; a fresh demo task avoids carrying them forward.

## Verification

Use isolated fixtures for error classification, redaction and no-write guarantees. Compare the same public-history fetch in restricted and approved-network contexts without using any live wallet. Preserve meaningful source and test evidence; implementation does not itself apply a live allocation. Actual results are recorded in the AI usage log.

Validation update: failure/history/orchestration tests passed 95/95 and CLI/routing regressions passed 15/15; typecheck, skill validation and whitespace checks passed. The same public-data preview reported network-unavailable in the restricted shell and completed with 249 observations under approved network access. Both disposable configurations remained unchanged. Tenjin removal was verified through absent registered shims/skills and a stopped daemon with no process record.
