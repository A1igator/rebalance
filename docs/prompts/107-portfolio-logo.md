# Text-free portfolio chart logo

Date: September 13, 2026.

During Clear Signing development, the owner requested a project logo matching the current portfolio pie chart without text, then specified a square 512 × 512 image. Export the existing vector geometry, palette and current displayed proportions; do not generate a different illustration or alter portfolio configuration.

The export uses the application's ringPath geometry, current outer/target weights from the local public chart status, and existing colors/ring widths. SVG and exactly 512 × 512 PNG variants with dark or transparent backgrounds were rendered and visually inspected. Files were delivered in the owner's local Downloads/rebalance-logo directory; no wallet address, view handle, status text or interactive controls are embedded. This is an image export, not a new transaction or a claim of verified live holdings beyond the chart's displayed state.
